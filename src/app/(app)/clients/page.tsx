import { EnConstruction } from "@/components/layout/en-construction";

export default function ClientsPage() {
  return (
    <EnConstruction
      titre="Clients"
      description="Carnet de clients (nom, téléphone, adresse, matricule fiscal) pour la facturation arrivera prochainement."
    />
  );
}
